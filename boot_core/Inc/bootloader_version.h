#ifndef BOOTLOADER_VERSION_H
#define BOOTLOADER_VERSION_H

#include <stdint.h>

static const uint8_t BOOTLOADER_VERS_MAJOR = 1;
static const uint8_t BOOTLOADER_VERS_MINOR = 0;
static const uint8_t BOOTLOADER_VERS_PATCH = 0;

#endif